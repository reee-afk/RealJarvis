JARVIS_DEMO=1
JARVIS_PORT=8420
JARVIS_HOST=127.0.0.1

# --- ElevenLabs (voice in/out) — required for voice, get one at elevenlabs.io ---
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=onwK4e9ZLuTAKqWW03F9

# --- Optional: Claude routing (leave blank to run on heuristic routing) ---
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# --- Optional: real vault (comma-separated absolute paths), only read when JARVIS_DEMO=0 ---
VAULT_PATHS=/Users/you/Documents/Clients,/Users/you/Notes

# --- Optional: read_inbox (IMAP, read-only) ---
EMAIL_IMAP_HOST=
EMAIL_USER=
EMAIL_PASSWORD=
